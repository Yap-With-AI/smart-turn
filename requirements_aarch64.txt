#
# DGX Spark / aarch64 install
#
# `torchcodec==0.8.1` currently has no PyPI wheel for aarch64, but HF `datasets` audio decoding
# requires `torchcodec` at runtime for this project (sample["audio"]["array"]).
#
# This requirements file keeps the project pins, but pulls `torchcodec` from the PyTorch nightly
# wheel index (typically the only place where aarch64 wheels are published).
#
# Notes:
# - `--pre` is required because nightly packages are pre-releases.
# - We intentionally do NOT pin `torchcodec` here: pip will select the newest compatible nightly.
#
--pre
--extra-index-url https://download.pytorch.org/whl/nightly/cu128

torch==2.9.0
torchaudio==2.9.0
transformers[torch]==4.48.2
datasets==4.4.1
scikit-learn==1.6.1
numpy==2.3.4
librosa
soundfile
wandb
torchcodec
onnx==1.19.1
onnxruntime-gpu==1.23.2
onnxscript==0.5.6

